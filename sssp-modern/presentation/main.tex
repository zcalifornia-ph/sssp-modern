% main.tex
% Copyright 2026 Zildjian E. California
%
% This file is part of the beamer-up derivative work and adapts the
% original beamer-uu sources by A.J.H. (Jos) Zuijderwijk to the
% maintained University of the Philippines `UP` theme surface.
%
% This work may be distributed and/or modified under the conditions of
% the LaTeX Project Public License, either version 1.3c of this license
% or (at your option) any later version. The latest version of this
% license is in
%   https://www.latex-project.org/lppl.txt
% and version 1.3c or later is part of all distributions of LaTeX
% version 2008 or later.
%
% This work has the LPPL maintenance status `maintained'.
%
% The Current Maintainer of this work is Zildjian E. California.
% Issue reports for this maintained derivative may be sent via
%   https://github.com/zcalifornia-ph/beamer-up/issues
%
% This work consists of the files beamercolorthemeUP.sty,
% beamerinnerthemeUP.sty, beamerouterthemeUP.sty, beamerthemeUP.sty,
% main.tex, and references.bib.
%
% Current checkout note: this deck instantiates the UP theme for the
% SSSP-modern CMSC 142 portfolio. This version contains the deck spine,
% baseline-algorithm frames, DMMSY centerpiece, experimental evaluation,
% reflection, and author-contribution slides; remaining work is limited to
% timed dry-run refinements.
% Branding note: distributed UP-branded assets follow the UP branding
% guidelines and are intended for UP constituents/internal use. No
% official endorsement is implied.

\documentclass[aspectratio=169]{beamer}
\usetheme[showlogo,english]{UP}

% Extra TikZ libraries for the worked-example visuals
\usetikzlibrary{arrows.meta, calc, fit, backgrounds}

% ===========================================
%  METADATA
% ===========================================
\title[SSSP: From Dijkstra to DMMSY]{Single-Source Shortest Paths:\\From Dijkstra to DMMSY}
\subtitle{\textbf{A CMSC 142 portfolio study comparing five SSSP algorithms in pure-Python stdlib, with DMMSY (2025) as the centerpiece.}}
\author[California \& Rizal]{%
  Zildjian E. California\inst{1} \and
  Rey Marvin C. Rizal\inst{1}%
}
\institute{Department of Mathematics, Physics, and Computer Science\\College of Science and Mathematics\\University of the Philippines - Mindanao}
\addinstitute{1}{Department of Mathematics, Physics, and Computer Science,\\College of Science and Mathematics, University of the Philippines - Mindanao}
\venue{CMSC 142 -- Design and Analysis of Algorithms}
\date{\today}
\multiauthor


\begin{document}
% ===========================================
%  TITLE SLIDE
% ===========================================
\begin{frame}[plain]
  \titlepage
\end{frame}
% Speaker note (California): name the paper centerpiece (DMMSY 2025) and
% the historical arc from Dijkstra 1959. Acknowledge Asc. Prof. Manliguez.
% Hand off to Rizal at "Five algorithms".

% ===========================================
%  OUTLINE
% ===========================================
\begin{frame}{Outline}
  \tableofcontents
\end{frame}
% Speaker note (California): point at the five sections; flag DMMSY as
% the centerpiece; flag Experimental Evaluation as the empirical claim.

% ===========================================
\section{Problem and Motivation}
% ===========================================

\begin{frame}{Single-Source Shortest Paths}
  \begin{block}{Problem}
    Given a weighted graph $G = (V, E)$ with edge weights $w \colon E \to \mathbb{R}$
    and a source vertex $s \in V$, compute:
    $$d(v) = \min_{\pi : s \to v} \sum_{e \in \pi} w(e) \qquad \text{for every } v \in V.$$
  \end{block}

  \begin{itemize}
    \item Edge orientation: directed for Dijkstra, Bellman--Ford, A*, DMMSY; undirected for Thorup.
    \item Weight class: non-negative reals for Dijkstra / A* / DMMSY; signed reals for Bellman--Ford; non-negative integers for Thorup.
    \item Output: a distance map $d \colon V \to \mathbb{R}$ (and, optionally, a predecessor map for path reconstruction).
  \end{itemize}
\end{frame}
% Speaker note (California): one-sentence framing, then read the bullet
% list as the "ground rules" for the comparison. Do not derive the
% algorithm here -- that is the next section.

\begin{frame}{Five Algorithms Across the Historical Arc}
  \begin{itemize}
    \item \textbf{Dijkstra (1959)}~\cite{dijkstra1959note} -- the foundational non-negative-weight algorithm; binary-heap relaxation.
    \item \textbf{Bellman--Ford (1958)}~\cite{bellman1958routing}, drawing on Ford (1956)~\cite{ford1956network} -- handles negative weights; detects negative cycles.
    \item \textbf{A\textsuperscript{*} (1968)}~\cite{hart1968astar} -- heuristic-guided search; degenerates to Dijkstra at $h \equiv 0$.
    \item \textbf{Thorup (1999)}~\cite{thorup1999linear} -- linear-time undirected SSSP for non-negative integer weights in the word-RAM model.
    \item \textbf{DMMSY (2025)}~\cite{duan2025breaking} -- deterministic $O(m \log^{2/3} n)$ directed SSSP; \alert{the centerpiece of this study}.
  \end{itemize}

  \vskip 6pt
  We re-implement all five in pure-stdlib CPython 3.11+ and benchmark each against Dijkstra on a documented suite.
\end{frame}
% Speaker note (California): stress the arc and the centerpiece; flag
% that DMMSY is what breaks the sorting barrier. Hand off to Rizal.

% ===========================================
\section{Baseline Algorithms}
% ===========================================

\begin{frame}{Dijkstra (1959) -- Heap Relaxation}
  \begin{columns}[T]
    \begin{column}{0.52\textwidth}
      \begin{block}{Problem Class}
        Non-negative weighted directed (or undirected) graphs; single source $s$.
      \end{block}
      \begin{block}{Design Idea}
        Maintain a frontier set $B$ in a min-heap keyed by tentative distance; repeatedly extract the closest unsettled vertex, settle it, and relax its outgoing edges.
      \end{block}
    \end{column}
    \begin{column}{0.44\textwidth}
      \begin{exampleblock}{Complexity}
        Time $O((n+m) \log n)$; space $O(n)$.
      \end{exampleblock}
      \begin{alertblock}{Implementation}
        \texttt{src/sssp/dijkstra.py}; \texttt{heapq}-backed;\\7 tests, 100\% line coverage.
        \\Source paper: \cite{dijkstra1959note}.
      \end{alertblock}
    \end{column}
  \end{columns}
\end{frame}
% Speaker note (California): "non-negative weights only" is the load-
% bearing assumption; this is why Bellman-Ford follows. Mention that
% Dijkstra is the BaseCase oracle for both DMMSY and the benchmark
% normalization.

\begin{frame}{Dijkstra -- A Worked Heap Relaxation}
  \begin{center}
  \resizebox{\textwidth}{!}{%
  \begin{tikzpicture}[
      every node/.style={font=\sffamily},
      vx/.style={circle, draw=UPBlack, line width=0.6pt, minimum size=0.85cm,
                 inner sep=0pt, font=\sffamily\bfseries},
      settled/.style={vx, fill=UPForestGreen, text=white, draw=UPForestGreen},
      frontier/.style={vx, fill=UPYellow, text=UPBlack, draw=UPYellow!80!UPBlack},
      unseen/.style={vx, fill=white, draw=UPBlack!50},
      we/.style={font=\footnotesize\sffamily, text=UPBlack80},
      ed/.style={->, >={Latex[length=2.2mm]}, semithick, UPBlack80},
      snap/.style={draw=UPBlack!25, rounded corners=2pt, inner sep=4pt,
                   font=\footnotesize\sffamily},
      stepttl/.style={font=\small\sffamily\bfseries, text=UPMaroon}
  ]
    % --- Graph (top) -------------------------------------------------
    \node[settled] (S0) at (0,0)   {S};
    \node[unseen]  (A0) at (2.2,0.9) {A};
    \node[unseen]  (B0) at (2.2,-0.9){B};
    \node[unseen]  (C0) at (4.4,0)   {C};
    \draw[ed] (S0) -- node[we, above left=-2pt] {3} (A0);
    \draw[ed] (S0) -- node[we, below left=-2pt] {5} (B0);
    \draw[ed] (A0) -- node[we, right=1pt]        {1} (B0);
    \draw[ed] (A0) -- node[we, above right=-2pt] {4} (C0);
    \draw[ed] (B0) -- node[we, below right=-2pt] {2} (C0);

    % --- Step 1 -----------------------------------------------------
    \node[stepttl] at (7.5, 0.95) {Step 1: pop S (d=0)};
    \node[snap, anchor=west] at (6.4, 0.0)
      {\begin{tabular}{@{}ll@{}}
         settled & \{S:0\} \\
         heap    & [(3,A),(5,B)] \\
         relaxed & S$\to$A, S$\to$B \\
       \end{tabular}};

    % --- Step 2 -----------------------------------------------------
    \node[stepttl] at (7.5, -1.55) {Step 2: pop A (d=3)};
    \node[snap, anchor=west] at (6.4, -2.5)
      {\begin{tabular}{@{}ll@{}}
         settled & \{S:0, A:3\} \\
         heap    & [(4,B),(5,B),(7,C)] \\
         relaxed & A$\to$B (5$\to$4), A$\to$C \\
       \end{tabular}};

    % --- Step 3 -----------------------------------------------------
    \node[stepttl] at (12.4, 0.95) {Step 3: pop B (d=4)};
    \node[snap, anchor=west] at (11.3, 0.0)
      {\begin{tabular}{@{}ll@{}}
         settled & \{S:0, A:3, B:4\} \\
         heap    & [(5,B$^*$),(6,C),(7,C)] \\
         relaxed & B$\to$C (7$\to$6) \\
       \end{tabular}};

    % --- Step 4 -----------------------------------------------------
    \node[stepttl] at (12.4, -1.55) {Step 4: pop C (d=6) -- done};
    \node[snap, anchor=west] at (11.3, -2.5)
      {\begin{tabular}{@{}ll@{}}
         settled & \{S:0,A:3,B:4,C:6\} \\
         heap    & stale entries skipped \\
         result  & all distances final \\
       \end{tabular}};

    % --- Connector arrows between steps -----------------------------
    \draw[->, >={Latex[length=2mm]}, UPMaroon, line width=0.6pt]
      (5.2,  0.4) -- (6.3,  0.4);
    \draw[->, >={Latex[length=2mm]}, UPMaroon, line width=0.6pt]
      (8.7, -0.4) -- (8.7, -1.05);
    \draw[->, >={Latex[length=2mm]}, UPMaroon, line width=0.6pt]
      (10.1, 0.4) -- (11.2, 0.4);
    \draw[->, >={Latex[length=2mm]}, UPMaroon, line width=0.6pt]
      (13.6,-0.4) -- (13.6,-1.05);
  \end{tikzpicture}
  }
  \end{center}

  \begin{center}
    \footnotesize
    \textcolor{UPForestGreen}{$\blacksquare$}~settled \quad
    \textcolor{UPYellow!80!UPBlack}{$\blacksquare$}~frontier \quad
    $B^*$~=~stale entry, ignored on pop
  \end{center}
\end{frame}
% Speaker note (California): walk left-to-right, top-to-bottom. The
% load-bearing intuition is that each pop settles one vertex and we
% never revisit it. Stale heap entries are skipped, not deleted -- this
% is the lazy-decrease-key pattern in heapq.

\begin{frame}{Bellman--Ford (1958, Ford 1956) -- Edge Relaxation}
  \begin{columns}[T]
    \begin{column}{0.52\textwidth}
      \begin{block}{Problem Class}
        Directed graphs with possibly negative edge weights; detect negative-weight cycles reachable from $s$.
      \end{block}
      \begin{block}{Design Idea}
        Repeat full-graph edge relaxation $n-1$ times; one extra pass diagnoses any reachable negative cycle.
      \end{block}
    \end{column}
    \begin{column}{0.44\textwidth}
      \begin{exampleblock}{Complexity}
        Time $O(n m)$; space $O(n)$.
      \end{exampleblock}
      \begin{alertblock}{Implementation}
        \texttt{src/sssp/bellman\_ford.py}; reports \texttt{NegativeCycleReport};\\100\% line coverage.
        \\Source papers: \cite{bellman1958routing}, \cite{ford1956network}.
      \end{alertblock}
    \end{column}
  \end{columns}
\end{frame}
% Speaker note (Rizal): emphasize that BF is the "negative weights"
% baseline and is reused conceptually inside DMMSY's FindPivots. Note
% that the curated negative-cycle fixtures verify the detection branch.

\begin{frame}{Bellman--Ford -- A Worked Round-by-Round Relaxation}
  \begin{center}
  \resizebox{\textwidth}{!}{%
  \begin{tikzpicture}[
      every node/.style={font=\sffamily},
      vx/.style={circle, draw=UPBlack, line width=0.6pt,
                 minimum size=0.85cm, inner sep=0pt,
                 font=\sffamily\bfseries},
      src/.style={vx, fill=UPForestGreen, text=white, draw=UPForestGreen},
      we/.style={font=\footnotesize\sffamily, text=UPBlack80},
      wneg/.style={font=\footnotesize\sffamily\bfseries, text=UPMaroon},
      ed/.style={->, >={Latex[length=2.2mm]}, semithick, UPBlack80},
      edneg/.style={->, >={Latex[length=2.2mm]}, line width=0.9pt, UPMaroon},
      snap/.style={draw=UPBlack!25, rounded corners=2pt, inner sep=4pt,
                   font=\footnotesize\sffamily},
      stepttl/.style={font=\small\sffamily\bfseries, text=UPMaroon}
  ]
    % --- Graph (left) -------------------------------------------------
    \node[src]  (S) at (0,0)     {S};
    \node[vx]   (A) at (1.8,1.2) {A};
    \node[vx]   (B) at (1.8,-1.2){B};
    \node[vx]   (C) at (3.8,0)   {C};
    \draw[ed]    (S) -- node[we, above left=-2pt] {4} (A);
    \draw[ed]    (S) -- node[we, below left=-2pt] {5} (B);
    \draw[edneg] (A) -- node[wneg, right=2pt]      {$-2$} (B);
    \draw[ed]    (B) -- node[we, below right=-2pt] {3} (C);
    \draw[ed]    (A) -- node[we, above right=-2pt] {5} (C);

    % --- Edge scan order -----------------------------------------
    \node[stepttl, anchor=west] at (5.6, 1.7) {Edge scan order};
    \node[snap, anchor=west] at (5.6, 0.0)
      {\begin{tabular}{@{}rl@{}}
         1. & (S,A,$+4$) \\
         2. & (S,B,$+5$) \\
         3. & (A,B,$-2$) \quad \textcolor{UPMaroon}{$\leftarrow$ key relaxation} \\
         4. & (B,C,$+3$) \\
         5. & (A,C,$+5$) \\
       \end{tabular}};

    % --- Round table on the right ------------------------------------
    \node[stepttl, anchor=west] at (10.6, 1.7)
      {Distance after each round};
    \node[snap, anchor=west] at (10.6, -0.4)
      {\begin{tabular}{@{}lcccc@{}}
         vertex     & init      & R1                & R2  \\ \hline
         S          & $0$       & $0$               & $0$ \\
         A          & $\infty$  & $4$               & $4$ \\
         B          & $\infty$  & $5 \rightarrow 2$ & $2$ \\
         C          & $\infty$  & $5$               & $5$ \\
       \end{tabular}};

    % --- Connector arrows --------------------------------------------
    \draw[->, >={Latex[length=2mm]}, UPMaroon, line width=0.6pt]
      (4.5, 0) -- (5.5, 0);
    \draw[->, >={Latex[length=2mm]}, UPMaroon, line width=0.6pt]
      (9.7, 0) -- (10.5, 0);
  \end{tikzpicture}
  }
  \end{center}

  \begin{itemize}
    \item Each round sweeps every edge once; relaxing $(A,B,-2)$ \emph{after} $(S,B,5)$ lowers $d[B]$ from $5$ to $2$.
    \item R2 produces no update $\Rightarrow$ early-exit; the final cycle-check pass also relaxes nothing $\Rightarrow$ \texttt{has\_cycle = False}.
  \end{itemize}
\end{frame}
% Speaker note (Rizal): the punchline is that order matters within a
% round but not across rounds -- after n-1 rounds every shortest
% simple path is discovered, and the extra pass diagnoses any reachable
% negative cycle. The "src/sssp/bellman_ford.py" early-exit on a
% no-update round is a documented deviation from the textbook variant.

\begin{frame}{A\textsuperscript{*} (1968) -- Heuristic Guidance}
  \begin{columns}[T]
    \begin{column}{0.52\textwidth}
      \begin{block}{Problem Class}
        Goal-directed shortest path with a domain-specific heuristic $h(v)$ lower-bounding $d(v, \mathrm{goal})$.
      \end{block}
      \begin{block}{Design Idea}
        Order the frontier by $f(v) = g(v) + h(v)$ instead of $g(v)$ alone; admissible $h$ preserves optimality.
      \end{block}
    \end{column}
    \begin{column}{0.44\textwidth}
      \begin{exampleblock}{Complexity}
        Time $O((n+m) \log n)$ worst case; expands fewer nodes than Dijkstra when $h$ is informative.
      \end{exampleblock}
      \begin{alertblock}{Implementation}
        \texttt{src/sssp/astar.py}, \texttt{src/sssp/heuristics.py}; ships \texttt{manhattan}, \texttt{zero}; \alert{$A^*(h\equiv 0) \equiv$ Dijkstra} tested.
        Source paper: \cite{hart1968astar}.
      \end{alertblock}
    \end{column}
  \end{columns}
\end{frame}
% Speaker note (California): the punchline is that A* generalizes
% Dijkstra; the zero-heuristic test makes that explicit. Mention the
% Manhattan grid demo as evidence of admissibility.

\begin{frame}{A\textsuperscript{*} -- A Worked Heuristic-Guided Search}
  \begin{center}
  \resizebox{\textwidth}{!}{%
  \begin{tikzpicture}[
      every node/.style={font=\sffamily},
      vx/.style={circle, draw=UPBlack, line width=0.6pt,
                 minimum size=0.95cm, inner sep=0pt,
                 font=\sffamily\bfseries},
      src/.style={vx, fill=UPForestGreen, text=white, draw=UPForestGreen},
      goal/.style={vx, fill=UPMaroon, text=white, draw=UPMaroon},
      starved/.style={vx, fill=UPMaroonTint, draw=UPBlack!30, text=UPBlack80},
      we/.style={font=\footnotesize\sffamily, text=UPBlack80},
      hlbl/.style={font=\footnotesize\sffamily\bfseries, text=UPForestGreen},
      ed/.style={->, >={Latex[length=2.2mm]}, semithick, UPBlack80},
      snap/.style={draw=UPBlack!25, rounded corners=2pt, inner sep=4pt,
                   font=\footnotesize\sffamily},
      stepttl/.style={font=\small\sffamily\bfseries, text=UPMaroon}
  ]
    % --- Grid chain (top row): S - A - B - C - G ---------------------
    \node[src]      (S) at (0.0,0)   {S};
    \node[vx]       (A) at (1.5,0)   {A};
    \node[vx]       (B) at (3.0,0)   {B};
    \node[vx]       (C) at (4.5,0)   {C};
    \node[goal]     (G) at (6.0,0)   {G};
    \node[starved]  (X) at (0.0,-1.6){X};

    \draw[ed] (S) -- node[we, above] {1} (A);
    \draw[ed] (A) -- node[we, above] {1} (B);
    \draw[ed] (B) -- node[we, above] {1} (C);
    \draw[ed] (C) -- node[we, above] {1} (G);
    \draw[ed] (S) -- node[we, left]  {5} (X);

    % h(v) labels under each grid cell
    \node[hlbl] at (0.0, 0.95) {$h{=}4$};
    \node[hlbl] at (1.5, 0.95) {$h{=}3$};
    \node[hlbl] at (3.0, 0.95) {$h{=}2$};
    \node[hlbl] at (4.5, 0.95) {$h{=}1$};
    \node[hlbl] at (6.0, 0.95) {$h{=}0$};
    \node[hlbl] at (0.0,-2.45) {$h{=}5$ (admissible, loose)};

    \node[font=\scriptsize\sffamily, text=UPBlack80] at (3.0,-1.6)
      {Manhattan heuristic to G; weights all $1$ on the chain};

    % --- Snapshots on the right --------------------------------------
    \node[stepttl, anchor=west] at (7.8, 0.95) {Pop S~~(f$=$0$+$4$=$4)};
    \node[snap, anchor=west] at (7.8, 0.0)
      {\begin{tabular}{@{}ll@{}}
         settled & \{S\} \\
         heap    & [(4,A), (10,X)] \\
         relaxed & S$\to$A (g$=$1), S$\to$X (g$=$5) \\
       \end{tabular}};

    \node[stepttl, anchor=west] at (12.6, 0.95) {Pop A~~(f$=$4)};
    \node[snap, anchor=west] at (12.6, 0.0)
      {\begin{tabular}{@{}ll@{}}
         settled & \{S, A\} \\
         heap    & [(4,B), (10,X)] \\
         relaxed & A$\to$B (g$=$2) \\
       \end{tabular}};

    \node[stepttl, anchor=west] at (7.8, -1.55)
      {Pops B then C~~(both f$=$4)};
    \node[snap, anchor=west] at (7.8, -2.5)
      {\begin{tabular}{@{}ll@{}}
         settled & \{S, A, B, C\} \\
         heap    & [(4,G), (10,X)] \\
         relaxed & B$\to$C, C$\to$G \\
       \end{tabular}};

    \node[stepttl, anchor=west] at (12.6, -1.55) {Pop G~~$=$~goal};
    \node[snap, anchor=west] at (12.6, -2.5)
      {\begin{tabular}{@{}ll@{}}
         path  & S, A, B, C, G \\
         cost  & $4$ \\
         X     & \emph{never popped} (f$=$10) \\
       \end{tabular}};

    % --- Connectors --------------------------------------------------
    \draw[->, >={Latex[length=2mm]}, UPMaroon, line width=0.6pt]
      (6.6, 0.4) -- (7.7, 0.4);
    \draw[->, >={Latex[length=2mm]}, UPMaroon, line width=0.6pt]
      (11.5, 0.4) -- (12.5, 0.4);
    \draw[->, >={Latex[length=2mm]}, UPMaroon, line width=0.6pt]
      (10.0, -0.45) -- (10.0, -1.10);
    \draw[->, >={Latex[length=2mm]}, UPMaroon, line width=0.6pt]
      (11.5, -2.1) -- (12.5, -2.1);
  \end{tikzpicture}
  }
  \end{center}

  \begin{itemize}
    \item Every chain vertex has $f = g + h = 4$, so A$^{*}$ pops them in order and \alert{never expands the dead-end branch~X} (its $f = 5 + 5 = 10$).
    \item Set $h \equiv 0$ and A$^{*}$ degenerates to Dijkstra -- the equivalence is asserted as a correctness test in \texttt{tests/test\_astar.py}.
  \end{itemize}
\end{frame}
% Speaker note (California): the load-bearing claim is that an
% admissible heuristic preserves optimality while pruning the frontier.
% On this example the heuristic is tight on the chain (h equals true
% remaining cost) and loose but still admissible at X, so X is
% effectively starved and never settled.

\begin{frame}{Thorup (1999) -- Linear-Time Undirected SSSP}
  \begin{columns}[T]
    \begin{column}{0.52\textwidth}
      \begin{block}{Problem Class}
        Undirected graphs with non-negative integer edge weights; word-RAM model.
      \end{block}
      \begin{block}{Design Idea}
        Hierarchical bucket structure exploiting integer weight bits and constant-time word operations to avoid the sorting bottleneck of comparison-based SSSP.
      \end{block}
    \end{column}
    \begin{column}{0.44\textwidth}
      \begin{exampleblock}{Complexity}
        Time $O(n + m)$ in the word-RAM model.
      \end{exampleblock}
      \begin{alertblock}{Implementation}
        \texttt{src/sssp/thorup99.py};\\11 tests, 99\% line coverage.\\\emph{Python $\ne$ word-RAM:} caveat documented in the project notes.\\Source paper: \cite{thorup1999linear}.
      \end{alertblock}
    \end{column}
  \end{columns}
\end{frame}
% Speaker note (Rizal): be explicit about the model gap -- Python's
% Big-Integer arithmetic does not preserve Thorup's linear-time
% guarantee. The implementation is faithful to the paper, not the
% asymptotic. This matters for the Experimental Evaluation discussion.

\begin{frame}{Thorup -- A Worked MSDB Bucket Walk}
  \begin{center}
  \resizebox{\textwidth}{!}{%
  \begin{tikzpicture}[
      every node/.style={font=\sffamily},
      vx/.style={circle, draw=UPBlack, line width=0.6pt,
                 minimum size=0.85cm, inner sep=0pt,
                 font=\sffamily\bfseries},
      src/.style={vx, fill=UPForestGreen, text=white, draw=UPForestGreen},
      we/.style={font=\footnotesize\sffamily, text=UPBlack80},
      uedge/.style={line width=0.7pt, UPBlack80},
      bkt/.style={rectangle, rounded corners=2pt, draw=UPBlack!40,
                  fill=white, minimum width=2.2cm, minimum height=0.7cm,
                  font=\footnotesize\sffamily, inner sep=3pt, align=center},
      bktlbl/.style={font=\footnotesize\sffamily\bfseries, text=UPMaroon},
      lminlbl/.style={font=\footnotesize\sffamily\bfseries, text=UPForestGreen},
      snap/.style={draw=UPBlack!25, rounded corners=2pt, inner sep=4pt,
                   font=\footnotesize\sffamily},
      stepttl/.style={font=\small\sffamily\bfseries, text=UPMaroon}
  ]
    % --- Graph (left) -------------------------------------------------
    \node[src] (S) at (0,0.6)   {S};
    \node[vx]  (A) at (1.7,1.5) {A};
    \node[vx]  (B) at (1.7,-0.3){B};
    \draw[uedge] (S) -- node[we, above left=-2pt] {1} (A);
    \draw[uedge] (S) -- node[we, below left=-2pt] {4} (B);
    \draw[uedge] (A) -- node[we, right=1pt]       {2} (B);
    \node[font=\scriptsize\sffamily, text=UPBlack80]
      at (1.0, -1.45) {undirected, integer weights};

    % --- Snapshot 1 ---------------------------------------------------
    \node[stepttl, anchor=west] at (3.6, 1.95) {After popping S};
    \node[lminlbl, anchor=west] at (3.6, 1.4) {last\_min~$=$~0};
    \node[bktlbl, anchor=west] at (3.6, 0.85) {bucket 1};
    \node[bkt, anchor=west]    at (4.7, 0.85) {A : $d{=}1$};
    \node[bktlbl, anchor=west] at (3.6, 0.20) {bucket 3};
    \node[bkt, anchor=west]    at (4.7, 0.20) {B : $d{=}4$};
    \node[font=\scriptsize\sffamily\itshape, anchor=west, text=UPBlack80]
      at (3.6, -0.45)
      {msdb($1,0$)$=$1; msdb($4,0$)$=$3};

    % --- Snapshot 2 ---------------------------------------------------
    \node[stepttl, anchor=west] at (7.6, 1.95) {Advance, settle A, relax};
    \node[lminlbl, anchor=west] at (7.6, 1.4) {last\_min~$=$~1};
    \node[bktlbl, anchor=west] at (7.6, 0.85) {bucket 0};
    \node[bkt, anchor=west, fill=UPForestGreen!18, draw=UPForestGreen]
                                at (8.7, 0.85) {A settled};
    \node[bktlbl, anchor=west] at (7.6, 0.20) {bucket 2};
    \node[bkt, anchor=west, fill=UPYellow!22, draw=UPYellow!80!UPBlack]
                                at (8.7, 0.20) {B : $4 \rightarrow 3$};
    \node[bktlbl, anchor=west] at (7.6,-0.45) {bucket 3};
    \node[bkt, anchor=west, fill=UPMaroonTint, draw=UPBlack!30]
                                at (8.7,-0.45) {B$^*$ stale};

    % --- Snapshot 3 ---------------------------------------------------
    \node[stepttl, anchor=west] at (11.6, 1.95) {Advance, settle B};
    \node[lminlbl, anchor=west] at (11.6, 1.4) {last\_min~$=$~3};
    \node[bktlbl, anchor=west] at (11.6, 0.85) {bucket 0};
    \node[bkt, anchor=west, fill=UPForestGreen!18, draw=UPForestGreen]
                                at (12.7, 0.85) {B settled};
    \node[bktlbl, anchor=west] at (11.6, 0.20) {result};
    \node[bkt, anchor=west, minimum width=2.4cm]
                                at (12.7, 0.20) {$d{=}\{S{:}0, A{:}1, B{:}3\}$};
    \node[font=\scriptsize\sffamily\itshape, anchor=west, text=UPBlack80]
      at (11.6, -0.45) {stale B$^*$ skipped on pop};

    % --- Connector arrows --------------------------------------------
    \draw[->, >={Latex[length=2mm]}, UPMaroon, line width=0.6pt]
      (7.0, 0.55) -- (7.5, 0.55);
    \draw[->, >={Latex[length=2mm]}, UPMaroon, line width=0.6pt]
      (11.0, 0.55) -- (11.5, 0.55);
  \end{tikzpicture}
  }
  \end{center}

  \begin{itemize}
    \item Buckets are keyed by $\mathrm{msdb}(d(v),\, \mathit{last\_min})$ -- the most-significant differing bit between a tentative label and the current minimum.
    \item Advancing \texttt{last\_min} re-bins live entries; lazy-stale entries (B$^*$) are skipped on pop. Word-RAM gives $O(m+n)$; CPython does not.
  \end{itemize}
\end{frame}
% Speaker note (Rizal): the bucket index is the only place integer-bit
% structure enters the algorithm -- this is what the paper trades the
% global heap for. On the word-RAM model the bit_length and bucket
% lookup are O(1); under CPython they are not, which is why the
% benchmark ratio sits above Dijkstra.

% ===========================================
\section{DMMSY (Centerpiece)}
% ===========================================

\begin{frame}{DMMSY (2025) -- Why It Matters}
  \small
  \begin{columns}[T]
    \begin{column}{0.52\textwidth}
      \begin{block}{Setting}
        Directed SSSP with non-negative real weights; labels use only comparison and addition.
      \end{block}
      \begin{block}{Barrier}
        Dijkstra-style methods still pay for global priority ordering, giving the sparse-graph comparison baseline $O(m + n \log n)$.
      \end{block}
    \end{column}
    \begin{column}{0.44\textwidth}
      \begin{exampleblock}{DMMSY Claim}
        Duan--Mao--Mao--Shu--Yin break the barrier deterministically:
        $$O\!\left(m \log^{2/3} n\right).$$
      \end{exampleblock}
      \begin{alertblock}{Portfolio Role}
        Centerpiece algorithm.\\Source paper: \cite{duan2025breaking}. \medskip \\Implementation: \texttt{src/sssp/dmmsy/}.
      \end{alertblock}
    \end{column}
  \end{columns}
\end{frame}
% Speaker note (California): make the model concrete: directed graph,
% non-negative real weights, only comparisons and additions. The reason
% this matters is that the paper breaks the sorting-style bottleneck
% that makes Dijkstra the historical baseline.

\begin{frame}{DMMSY Architecture}
  \begin{center}
  \resizebox{\textwidth}{!}{%
  \begin{tikzpicture}[
    node distance=0.7cm and 0.7cm,
    box/.style={
      rectangle, rounded corners=3pt, minimum height=1.7cm,
      text width=3.1cm, align=center,
      font=\small\sffamily\bfseries, draw=none
    },
    arr/.style={->, >={Latex[length=2.6mm]}, line width=0.9pt, UPBlack80}
  ]
    \node[box, fill=UPMaroon, text=white] (transform)
      {Constant-degree\\transform\\[2pt]{\scriptsize\mdseries paper \S2}};
    \node[box, fill=UPForestGreen, text=white, right=of transform] (pivots)
      {FindPivots\\[2pt]{\scriptsize\mdseries Algorithm 1}};
    \node[box, fill=UPYellow, text=UPBlack, right=of pivots] (bmssp)
      {BMSSP\\recursion\\[2pt]{\scriptsize\mdseries Algorithms 2--3}};
    \node[box, fill=UPBlack, text=white, right=of bmssp] (blocklist)
      {BlockList\\frontier\\[2pt]{\scriptsize\mdseries Lemma 3.3}};

    \draw[arr] (transform) -- (pivots);
    \draw[arr] (pivots) -- (bmssp);
    \draw[arr] (bmssp) -- (blocklist);
    \draw[arr, dashed, UPForestGreen, shorten <=2pt, shorten >=2pt]
      (blocklist.north) to[out=135, in=45, looseness=1.2]
      node[midway, above=1pt, font=\footnotesize\sffamily\bfseries,
           text=UPForestGreen, fill=white, inner sep=2pt] {bounded pulls}
      (bmssp.north);
  \end{tikzpicture}
  }
  \end{center}

  \vskip 4pt
  \begin{itemize}
    \item \textbf{Transform:} reduce high-degree vertices so work charges uniformly per arc.
    \item \textbf{FindPivots:} shrink the frontier to useful recursion roots.
    \item \textbf{BMSSP + BlockList:} solve bounded subproblems without globally sorting every label.
  \end{itemize}
\end{frame}
% Speaker note (California): walk left to right. The key message is not
% that one component is magic; the speedup comes from making frontier
% work local and bounded instead of globally sorted.

\begin{frame}{BMSSP Recursion}
  \footnotesize
  \begin{block}{Contract}
    \textsc{BMSSP}$(G,\ell,B,S,d,k,t)$ solves labels below boundary $B$ from source set $S$, then returns a smaller boundary $B'$ and settled set $U$.
  \end{block}

  \begin{columns}[T]
    \begin{column}{0.47\textwidth}
      \begin{exampleblock}{Level 0}
        \textsc{BaseCase}: bounded Dijkstra from one source, stopping after $k+1$ discoveries or boundary exhaustion.
      \end{exampleblock}
    \end{column}
    \begin{column}{0.49\textwidth}
      \begin{alertblock}{Level > 0}
        \begin{enumerate}
          \item \textsc{FindPivots} shrinks $S$ to pivots $P$.
          \item BlockList pulls bounded batches below $B$.
          \item Recursive calls settle batches and relax outgoing edges.
        \end{enumerate}
      \end{alertblock}
    \end{column}
  \end{columns}

  \vskip 4pt
  \textbf{Key idea:} local bounded batches replace one global sorted frontier.
\end{frame}
% Speaker note (California): do not read this as code. Explain the
% recursion contract: each call solves only what is below a boundary,
% returns a separator, and lets the next call continue from there.

\begin{frame}{DMMSY -- Pulling One Bounded Batch}
  \begin{center}
  \resizebox{\textwidth}{!}{%
  \begin{tikzpicture}[
      every node/.style={font=\sffamily},
      lab/.style={circle, draw=UPBlack, line width=0.6pt,
                  minimum size=0.95cm, inner sep=0pt,
                  font=\small\sffamily\bfseries, fill=white},
      settled/.style={lab, fill=UPForestGreen, text=white,
                      draw=UPForestGreen},
      deferred/.style={lab, fill=UPMaroonTint, draw=UPBlack!30, text=UPBlack80},
      bnd/.style={dashed, line width=1pt, UPMaroon},
      arr/.style={->, >={Latex[length=2.6mm]}, line width=0.8pt},
      lbl/.style={font=\footnotesize\sffamily, text=UPBlack80}
  ]
    % --- Boundary line B ----------------------------------------------
    \draw[bnd] (-0.4,2.0) -- (14.2,2.0)
      node[right, font=\small\sffamily\bfseries, text=UPMaroon] {$B = 10$};

    % --- Above-boundary labels (frozen until a smaller B') ------------
    \node[deferred] (u4) at (1.2, 2.95) {12};
    \node[deferred] (u6) at (3.6, 3.10) {14};
    \node[deferred] (u7) at (6.2, 2.85) {11};
    \node[lbl] at (8.6, 3.0)
      {\textit{above $B$: deferred, not pulled}};

    % --- Pool below B: tentative labels ------------------------------
    \node[lab] (u1) at (1.0, 0.85) {4};
    \node[lab] (u2) at (2.6, 0.55) {6};
    \node[lab] (u3) at (4.2, 0.95) {8};
    \node[lab] (u5) at (5.8, 0.30) {9};
    \node[lab] (u8) at (7.0, 1.15) {9};

    \node[lbl] at (1.0, -0.1) {$u_1$};
    \node[lbl] at (2.6, -0.4) {$u_2$};
    \node[lbl] at (4.2, 0.0)  {$u_3$};
    \node[lbl] at (5.8,-0.65) {$u_5$};
    \node[lbl] at (7.0, 0.2)  {$u_8$};

    % --- The pulled batch (the k+1 smallest below B) ------------------
    \begin{scope}[on background layer]
      \node[draw=UPYellow!80!UPBlack, line width=0.9pt,
            rounded corners=4pt, fill=UPYellow!18,
            fit=(u1)(u2)(u3), inner sep=8pt] (batch) {};
    \end{scope}
    \node[font=\footnotesize\sffamily\bfseries, text=UPBlack,
          above=2pt of batch.north] {Pull $k+1$ smallest below $B$};

    % --- Settled batch on the right ----------------------------------
    \node[settled] (s1) at (10.2, 1.05) {4};
    \node[settled] (s2) at (11.6, 0.75) {6};
    \node[settled] (s3) at (13.0, 1.05) {8};
    \node[lbl] at (10.2, 0.05) {$u_1$};
    \node[lbl] at (11.6,-0.25) {$u_2$};
    \node[lbl] at (13.0, 0.05) {$u_3$};
    \node[font=\footnotesize\sffamily\bfseries, text=UPForestGreen]
      at (11.6, 1.95) {Settle + relax outgoing edges};

    % --- Big arrow batch -> settled ----------------------------------
    \draw[arr, UPMaroon] (batch.east) -- ++(0.6,0)
      -- node[above, font=\footnotesize\sffamily\bfseries, text=UPMaroon]
         {BaseCase} ($(s1.west)+(-0.4,0)$) -- (s1.west);

    % --- Relaxation arrows feeding new tentative labels back ----------
    \draw[arr, UPForestGreen, dashed]
      (s2.south) .. controls (11.0,-0.9) and (8.5,-0.9) .. (u5.south)
      node[pos=0.55, below=1pt, font=\footnotesize\sffamily,
           text=UPForestGreen] {relax: $9 \to 8$};

    % --- New boundary B' ---------------------------------------------
    \draw[bnd, UPForestGreen]
      (-0.4,-1.45) -- (14.2,-1.45)
      node[right, font=\small\sffamily\bfseries, text=UPForestGreen]
        {$B' = 8 < B$};
  \end{tikzpicture}
  }
  \end{center}

  \begin{itemize}
    \item Pull only the $k+1$ smallest tentative labels below~$B$ -- no global sort.
    \item Settle, relax, return a tighter boundary~$B' < B$; deferred labels stay above~$B$.
  \end{itemize}
\end{frame}
% Speaker note (California): the speedup comes from never sorting the
% whole frontier. Each pull is local and bounded; the relaxation feeds
% smaller labels back into the pool; the new boundary B' is tighter
% than B, which is what drives recursion downward.

\begin{frame}{Parameters, Bound, and Implementation Caveat}
  \footnotesize
  \begin{columns}[T]
    \begin{column}{0.48\textwidth}
      \begin{exampleblock}{Paper Parameterization}
        \[
          k = \left\lfloor (\log_2 n)^{1/3} \right\rfloor
        \]
        \[
          t = \left\lfloor (\log_2 n)^{2/3} \right\rfloor,\qquad
          \ell_{\max} = \left\lceil \frac{\log_2 n}{t} \right\rceil
        \]
        Balances pivot work, recursion depth, and block-list operations.
      \end{exampleblock}
    \end{column}
    \begin{column}{0.48\textwidth}
      \begin{block}{Asymptotic Result}
        Deterministic directed SSSP:\medskip
        \[
          O\!\left(m \log^{2/3} n\right)\ \text{time}
        \]
        \\Space stays $O(n+m)$.
      \end{block}
      \begin{alertblock}{Python Implementation Boundary}
        CPython recursion overhead is real. Default large numeric benchmark runs use the documented shortcut; \texttt{Weight} inputs and explicit parameters exercise recursive BMSSP.
      \end{alertblock}
    \end{column}
  \end{columns}
\end{frame}
% Speaker note (California): separate theory from implementation
% honestly. The paper result is the centerpiece; the portfolio proves a
% faithful implementation surface while acknowledging CPython overhead.

% ===========================================
\section{Experimental Evaluation}
% ===========================================

\begin{frame}{Benchmark Protocol}
  \small
  \begin{columns}[T]
    \begin{column}{0.46\textwidth}
      \begin{block}{Dataset}
        Seed 2026; 12 benchmark cases; geometric and Barabasi--Albert families at
        $n \in \{64, 256, 1024\}$ with sparse/dense profiles.
      \end{block}
      \begin{block}{Timing}
        60 CSV rows; setup and file IO excluded; one recorded repeat per
        case-algorithm pair; Dijkstra is the normalization baseline.
      \end{block}
    \end{column}
    \begin{column}{0.50\textwidth}
      \begin{center}
        \includegraphics[width=\linewidth,height=0.56\textheight,keepaspectratio]{../bench/figures/runtime-by-algorithm.pdf}
      \end{center}
    \end{column}
  \end{columns}
\end{frame}
% Speaker note (Rizal): establish what was measured before interpreting
% the chart. The important honesty point is that Thorup uses the
% integer-undirected graph view and that Dijkstra normalizes every row.

\begin{frame}{Results Against Dijkstra}
  \footnotesize
  \begin{block}{Median runtime ratio by size bucket}
    \begin{center}
    \begin{tabular}{lccc}
      \hline
      Algorithm & Small $n=64$ & Medium $n=256$ & Large $n=1024$ \\
      \hline
      Dijkstra & $1.00\times$ & $1.00\times$ & $1.00\times$ \\
      Bellman--Ford & $1.92\times$ & $3.48\times$ & $3.87\times$ \\
      A\textsuperscript{*} & $0.80\times$ & $0.29\times$ & $0.50\times$ \\
      Thorup (1999) & $1.84\times$ & $1.61\times$ & $1.36\times$ \\
      DMMSY (2025) & $11.28\times$ & $5.87\times$ & $1.02\times$ \\
      \hline
    \end{tabular}
    \end{center}
  \end{block}

  \begin{itemize}
    \item A* benefits from an informative geometric heuristic; Bellman--Ford pays for repeated edge sweeps.
    \item Thorup stays above Dijkstra because CPython is not the word-RAM model.
    \item DMMSY passes the large-size runtime sanity check: max DMMSY/Dijkstra ratio is \texttt{1.093x} for $n \ge 1000$.
  \end{itemize}
\end{frame}
% Speaker note (Rizal): read only the pattern, not every cell. Close
% with the precise DMMSY caveat: the large-size ratio passes because
% default numeric inputs use the documented shortcut.

\begin{frame}{Result Evidence}
  \footnotesize
  \begin{block}{Notable Artifacts}
    \begin{itemize}
      \item Github repository: \url{https://github.com/zcalifornia-ph/sssp-modern}
      \item CSV: \texttt{bench/results/sssp-benchmark-seed-2026.csv}
      \item Figure: \texttt{bench/figures/runtime-by-algorithm.\{pdf,png\}}
      \item Tests: 147-test suite passed after benchmark plot generation; the runtime check records max DMMSY/Dijkstra ratio \texttt{1.093x} for $n \ge 1000$.
      \item Report anchors: Table \texttt{tab:results-by-class} and Figure \texttt{fig:runtime-by-algorithm}.
    \end{itemize}
  \end{block}

  \begin{alertblock}{Final Words}
    The portfolio centers the algorithmic result and implementation structure,
    not a CPython timing victory: correctness and comparison-addition fidelity
    are validated separately from the fast-path benchmark policy.
  \end{alertblock}
\end{frame}

% ===========================================
\section{Reflection and Author Contributions}
% ===========================================

\begin{frame}{Reflection and Integrity}
  \small
  \begin{block}{What The Comparison Teaches}
    No SSSP algorithm dominates every setting; the right choice depends on
    weight signs, direction, integer assumptions, heuristic quality, and runtime model.
  \end{block}

  \begin{alertblock}{Honest DMMSY Framing}
    The centerpiece is the deterministic $O(m \log^{2/3} n)$ comparison-addition
    bound. CPython does not show a recursive BMSSP wall-clock win; large numeric
    runs use the documented shortcut to keep the runtime check passing.
  \end{alertblock}

  \begin{exampleblock}{AI Assistance Disclosure}
    AI tools assisted with drafting, tests, supporting files, and prose. The source
    papers, validation evidence, and final judgments remain the human authors'
    responsibility.
  \end{exampleblock}
\end{frame}
% Speaker note (California): summarize usefulness and limitations
% without overclaiming. Rizal can add that the benchmark evidence is
% useful because it records what CPython actually does.

\begin{frame}{Author Contributions}
  \footnotesize
  \begin{columns}[T]
    \begin{column}{0.48\textwidth}
      \begin{block}{Zildjian E. California}
        \begin{itemize}
          \item Repository foundation, root documentation, graph/weight/IO/generator base, Dijkstra, A*, DMMSY, benchmarks,\\report, and deck authorship.
          \item DMMSY centerpiece: transform, block list, pivots, BMSSP, driver, written rationale, and presentation opening/centerpiece.
          \item Current commit history: authored the foundation, Dijkstra/A*, DMMSY stack, benchmark files, report manuscript, and deck scaffold.
        \end{itemize}
      \end{block}
    \end{column}
    \begin{column}{0.48\textwidth}
      \begin{block}{Rey Marvin C. Rizal}
        \begin{itemize}
          \item Bellman--Ford implementation with negative-cycle reporting; assigned contribution completed.
          \item Thorup 1999 runtime-model work and hierarchical-bucket SSSP contribution; assigned contribution completed.
          \item Presentation ownership: Bellman--Ford, Thorup, and experimental-results delivery; joint Q\&A with California.
        \end{itemize}
      \end{block}
    \end{column}
  \end{columns}

  \vskip 1pt
  \begin{center}
    \scriptsize Contribution split follows the GitHub repository commit history and the closed issues and pull requests.
  \end{center}
\end{frame}
% Speaker note (joint): use this slide to make authorship legible,
% not ceremonial. California states the commit-history basis; Rizal
% confirms the assigned Bellman-Ford and Thorup contributions.

% -- Closing -----------------------------------
\thankframe{Questions?}{%
  Zildjian E. California \quad github.com/zcalifornia-ph\\
  Rey Marvin C. Rizal \quad github.com/marverickdev\\
  \url{https://github.com/zcalifornia-ph/sssp-modern}%
}
% Speaker note (joint): leave the thankframe up during Q&A; both
% authors field questions; California fields DMMSY questions, Rizal
% fields experimental-evaluation questions.

% -- references -----------------------------
\begin{frame}[allowframebreaks]{References}
  The five source papers cited above are listed below; the Beamer class~\cite{tantau2004} and the UP Visual Identity Guidebook~\cite{upvig2017} are credited for the deck infrastructure.

  \vskip 6pt
  \printbibliography
\end{frame}
% Speaker note (joint): this is not part of the timed talk. Use it only
% if the Q&A asks for paths, test evidence, or the DMMSY timing caveat.

\end{document}
